# Architecture

Docket is a personal job application tracker: React SPA on Cloudflare Pages talking to a Hono API on Cloudflare Workers, backed by Neon Postgres (Hyperdrive) and R2 for documents.

## Components

| Piece | Tech | Host |
|---|---|---|
| Web | React 19 + Vite + Tailwind v4 + shadcn/ui | Cloudflare Pages |
| API | Hono + Better Auth + Drizzle | Cloudflare Workers |
| DB | Neon Postgres (staging/main branches) | via Hyperdrive |
| Files | R2 buckets (presigned URLs) | Cloudflare R2 |

## Request flow

1. Browser loads SPA from Pages custom domain.
2. Auth cookies are set on the app cookie domain so Sessions work across Pages ↔ Workers.
3. API routes authenticate via Better Auth session, then open a Drizzle client over Hyperdrive with the Neon role JWT so RLS `auth.user_id()` matches the session user.
4. Document upload/download uses short-lived R2 signed URLs generated by the Worker.

## Monorepo

- `apps/web` — SPA
- `apps/api` — Worker
- `packages/shared` — Zod schemas and shared types

## Out of scope (v1)

- Resend reminder digests (documented for later)
- Multi-user collaboration UI (RLS is scaffolded for it)
